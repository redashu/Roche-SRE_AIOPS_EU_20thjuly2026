apiVersion: v1
kind: Service
metadata:
  name: roche-chatapp
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 5015
  selector:
    app: roche-chatapp
  type: LoadBalancer
status:
  loadBalancer: {}
